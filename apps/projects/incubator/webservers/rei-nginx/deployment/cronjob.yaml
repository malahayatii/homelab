apiVersion: batch/v1
kind: CronJob
metadata:
  name: rei-nginx-sync
  namespace: rei-nginx
spec:
  schedule: "*/1 * * * *"  # Cron expression for running every 5 minutes
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: rei-nginx
        spec:
          containers:
          - name: rei-nginx-sync
            image: moffoso/sync-rei-nginx:0.0.7
            volumeMounts:
            - name: rei-nginx-data
              mountPath: /tmp/new
          volumes:
          - name: rei-nginx-data
            hostPath:
              path: /media/kubevirt-hpp/pvc-6bc806fd-12e7-4719-b2c0-c616c8a6a742
          restartPolicy: OnFailure
