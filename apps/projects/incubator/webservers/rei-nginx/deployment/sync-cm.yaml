apiVersion: v1
kind: ConfigMap
metadata:
  name: sync-script
data:
  sync.sh: |
    #!/bin/bash
    GIT_URL="https://github.com/rk-webdesign/tales.git"
    SOURCE_PATH="/home/sync/git/tales/website/"
    echo "syncing $SOURCE_PATH to destination"
    rsync -r --delete "$SOURCE_PATH" /tmp/website/